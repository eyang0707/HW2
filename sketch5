void setup(){
  size(500,500);
  background(0);
  rectMode(CORNERS);
}     
int i = 0;
//Loop
void draw(){
  fill(0);
  rect(i , 500, i + 5, 0);
  float r = random(0,500);
  fill(255);
  rect(i , 500, i + 5, r);
  i += 10;
  if(i>500){
    i = 0;
  }
}
